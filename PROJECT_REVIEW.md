# 项目扫描与评分

扫描日期：2026-09-10。范围：`D:\lvgl` 自有固件、构建配置、辅助工具、现有测试和文档；第三方依赖未逐行审计。未修改业务代码，未烧录设备或重新进行真机验收。以下评分是基于当前工作区的工程评估，不是自动化工具计算的质量指数。

> 2026-09-10 整改：报告列出的五项问题均已在代码中处理，包括运行期 WiFi 重连、正文/索引提交顺序及旧状态修复、反向跨章定位、在线缓存隔离和章内偏移恢复。随后加入 TF 在线章节归档、TF 书库、任意长度 UTF-8 TXT 按需分页与断点续读。修改已通过固件构建、主机自测及真机启动验收；32GB TF 卡已成功挂载。下方 76 分保留为整改前基线。
>
> 2026-09-13 复核：逐条回看代码确认第 1、3、4、5 项已落实（见各条下的复核结论），第 2 项为部分修复。本轮另做了一次覆盖错误路径、任务竞态与文档一致性的静态审查，新发现的问题不在本文件五项之列，另文记录，不改变本条目的历史评分性质。

## 总评：76 / 100

项目定位为经典 ESP32 上的个人小说阅读器。搜索、拼音输入、在线下载、分页、滑动缓存、书架及亮度保存已经形成完整功能链路；内存控制考虑较充分。主要扣分来自异常恢复、阅读细节及工程化验证不足。适合作为持续迭代的个人项目，尚不足以据此认定为可长期无人干预运行的成熟产品。

| 维度 | 得分 | 依据 |
| --- | ---: | --- |
| 功能完整性 | 18 / 20 | 核心功能链路齐全；页内进度恢复和离线书籍管理仍有限制 |
| 资源与性能设计 | 17 / 20 | 五章窗口、分页偏移、逐网页写盘、gzip 大块分配策略；未重新测量峰值堆和长期碎片 |
| 可靠性与异常恢复 | 12 / 20 | 有临时文件、备份回滚、CRC 和重试；断网重连与正文/索引一致性存在缺口 |
| 架构与可维护性 | 15 / 20 | HTML 与缓存计算可独立测试；抓取模块 1662 行，混合网络、存储、目录和任务状态 |
| 测试、构建与文档 | 14 / 20 | 主机测试、样本和依赖锁文件可用；无统一测试入口/CI，当前目录没有 Git 元数据，文档混有历史行为 |
| 合计 | **76 / 100** | |

## 优先问题

### 1. P1：联网成功后的断线没有自动重连路径 —— 已修复（2026-09-13 复核）

复核：`wifi_setup_ui.c` 已加入独立的运行期重连任务，退避 0.5→8 秒封顶，并与主动停止区分。以下为整改前的原始分析。

位置：`main/wifi_setup_ui.c:94`，`main/wifi_setup_ui.c:217`。

断开事件只清除连接标志并设置失败位。`esp_wifi_connect()` 出现在 STA 启动事件和首次连接任务的有限重试中，成功后连接任务结束。阅读中路由器短暂掉线再恢复时，后台 HTTP 重试并不重新建立 WiFi 连接；选书/阅读界面也未提供返回 WiFi 设置的入口。

建议：增加独立的运行期重连状态机，区分主动停止与意外断线，采用有上限的退避间隔，并将连接状态暴露给 UI。验证路由器断电恢复、AP 暂时不可达和切换界面期间的断线。

### 2. P1：正文成功发布、下一章索引追加失败后，重试可能永久跳过故障章 —— 部分修复（2026-09-13 复核）

复核：提交顺序已反过来，现在先提交章节索引、后发布正文（`web_scraper.c:1581` 与 `:1593`），原缺口不再成立；但该处注释与恢复分支（`web_scraper.c:1723-1734`）仍按“先正文、后索引”描述，二者不一致，恢复逻辑护不住它声明要护的场景，并会对边界章多做一次无谓的重新下载。以下为整改前的原始分析。

位置：`main/web_scraper.c:1361`、`:1371`、`:1469`。

`download_biquge_chapter()` 先发布正式 `.txt`，再追加下一章 ID。若后一步因写盘错误失败，正文已经存在。后续任务只选择没有正文文件的章节，因此不再重新执行该章的下一章发现操作。即使写盘条件恢复，目录也可能停在当前末端，界面却进入“已缓存”状态。

这是由代码控制流确认的缺口，本次未做设备故障注入。建议把“正文就绪”和“后继索引已提交”分别建模，保留待补交状态；补充正文成功而索引失败、索引短写、重启恢复等测试。

### 3. P2：跨章向前翻页会进入上一章第一页 —— 已修复（2026-09-13 复核）

复核：`novel_reader.c:689`、`:766` 在反向跨章时定位到上一章末页，异步等待章节到达后也保留该方向。以下为整改前的原始分析。

位置：`main/novel_reader.c:505`、`:762`、`:765`。

无论向前还是向后打开章节，加载函数都会将 `current_page` 重置为 0。用户在第 N 章第一页点击上一页，实际看到第 N−1 章第一页，无法连续反向阅读。在线和本地路径都有此行为。

建议：给章节切换传递方向，向前跨章时定位末页；异步等待章节完成后也保留这一方向。

### 4. P2：本地章节与在线缓存共用文件名和删除范围 —— 已修复（命名空间隔离，2026-09-13 复核）

复核：在线缓存改用 `cache_%03d.txt` 独立命名，`prune_chapter_cache` 只匹配该前缀及其 `.tmp`/`.bak`，本地 `%03d.txt` 不再受牵连；缓存归属已持久化。完整 `idf.py flash` 覆盖 SPIFFS 的行为仍在，属烧录方式约束，已在 HANDOFF 5.3 与第 8 节注明。以下为整改前的原始分析。

位置：`main/web_scraper.c:1409`，`CMakeLists.txt:8`。

首次打开在线书籍或切换书籍时，`prune_chapter_cache(0, 0)` 会删除 SPIFFS 中所有符合命名格式的数字章节，包括随固件烧录的本地小说。缓存归属只在 RAM 中保存，重启后也不能确认并复用原在线缓存。README 提到了清理行为，因此这是明确的产品设计限制，不应误称为未披露的随机数据丢失。

另外，完整 `idf.py flash` 会按配置烧录 SPIFFS 镜像并覆盖运行期缓存与目录索引；历史交接记录中的“保留 SPIFFS”只适用于应用分区单独烧录等操作。建议隔离本地章节与缓存命名空间、持久化缓存归属，并在更新说明中明确烧录范围。

### 5. P2：进度保存只精确到章 —— 已修复（2026-09-13 复核）

复核：进度保存已改为章号 + 正文 UTF-8 字节偏移，翻页停止两秒后合并写一次 NVS，重进书籍可恢复到章内页面。以下为整改前的原始分析。

位置：`main/novel_reader.c:505`、`:509`，`main/bookshelf.h`。

书架保存章号，没有页码或正文偏移。重启或离开阅读页再返回时，会从本章开头重读。这与“章节进度”的文档表述一致，但对长章节体验影响明显。建议保存正文偏移并进行延迟合并写入，避免每次翻页都提交 NVS。

## 本次验证

- 重新编译并执行 `chapter_cache_selftest`：通过，覆盖 9 组窗口边界。
- 重新编译并执行 `web_html_selftest`：通过，包含已有网页样本、合成搜索/正文/分页场景。
- 以上测试及 `book_source_probe` 使用 GCC 的 `-std=c11 -Wall -Wextra -Wpedantic` 编译，本次未输出编译警告。
- `book_source_probe` 使用磁盘已有的搜索、详情、正文样本：通过，解析 8 个搜索结果、首章 ID 和同章下一页链接。这不等同于当前线上书源可用性验证。
- 重新解析组件依赖后完成全量构建，并再次通过 Ninja 增量构建；应用固件为 `0x1b7ef0`（1,801,968 字节），应用分区剩余 `0x38110`（229,648 字节，约 11%）。（该组数字对应当时 1984KB 的 app 分区；2026-09-13 分区表已扩至 3MB，当前固件 1,888,048 字节，余约 1.20MiB。）
- 常规 `export.bat` 后直接调用 `idf.py` 的尝试未成功解析命令；随后使用已配置的 Ninja 和显式 IDF 路径完成上述验证。
- README/HANDOFF 中的真机测试属于历史记录。本次未验证触摸手感、屏幕布局、长时间内存稳定性、Flash 磨损或断电恢复。

## 建议实施顺序

剩余工作集中在工程化验证：把现有测试接入统一命令，增加异常状态机测试、TF 拔插/满盘/断电测试、Git 版本管理和长期运行监测。文档应继续区分当前实现与历史验收，并明确完整烧录的数据覆盖范围。

本次新增此报告和 `build/audit_*.exe` 测试产物，增量构建更新了构建产物；未改动固件业务逻辑。

## 主机测试复现命令

三个测试目前没有统一入口，需在仓库根目录下分别执行（样本按相对路径打开）：

```bash
gcc -std=c11 -Wall -Wextra -Wpedantic -o /tmp/web_html_selftest tools/web_html_selftest.c main/web_html.c && /tmp/web_html_selftest
gcc -std=c11 -Wall -Wextra -Wpedantic -I main -o /tmp/chapter_cache_selftest tools/chapter_cache_selftest.c main/chapter_cache.c && /tmp/chapter_cache_selftest
gcc -std=c11 -Wall -Wextra -Wpedantic -o /tmp/book_source_probe tools/book_source_probe.c main/web_html.c && /tmp/book_source_probe build/source_search.html build/source_detail.html build/source_chapter.html
```

注意：`book_source_probe` 的输入样本位于 `build/` 目录内，`idf.py fullclean` 后需重新抓取样本才能运行。建议后续把这套命令收进 `tools/run_tests.sh`，并把样本移出 `build/`。
